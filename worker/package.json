{
  "name": "casttv-worker",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.mjs",
    "lint": "prettier --check --no-error-on-unmatched-pattern 'src/**/*.{js,mjs}' 'test/**/*.{js,mjs}' package.json",
    "format": "prettier --write --no-error-on-unmatched-pattern 'src/**/*.{js,mjs}' 'test/**/*.{js,mjs}' package.json"
  },
  "devDependencies": {
    "prettier": "^3.3.0",
    "wrangler": "^4.0.0"
  }
}
